1.Словарь (dict) — это изменяемая структура данных в Python, хранящая пары ключ–значение.
2. Пустой словарь создаётся с помощью {} или dict(), элементы добавляются через присваивание по ключу.
3. Основные методы: get(), keys(), values(), items(), update(), pop().
4. Значение получают через dict[key] или dict.get(key). Если ключа нет — dict[key] вызывает ошибку, get() возвращает None или заданное значение по умолчанию.
5. Перебор выполняется через цикл for, используя dict.items() для пар ключ–значение.
6. dict.get() безопасен при отсутствии ключа — не вызывает ошибку, а возвращает None или заданное значение. Прямое обращение dict[key] вызывает KeyError
